#CDN服务域名
#domain_cdn = http://7xign9.com1.z0.glb.clouddn.com

#静态资源服务域名
#domain_static = http://127.0.0.1

#存储0本地存储，1将启动7牛，自己也可以实现 ,路由配置中：添加 me.add("/upload", UploadQiniuController.class);覆盖系统的存储支持
oss_static=1
#资源服务域名（比如7牛 启用云存储，否则将会用 domain_img 和  domain_file参数）
domain_oss_static = http://img.bblocks.cn

#######################目录配置
#静态根目录(图片上传路径)
static_root = D:/eova/static2

#最大上传（KB）
UPLOAD_IMG_MAX = 5120
UPLOAD_FILE_MAX = 5120

#上传图片服务域名
domain_img = http://127.0.0.1:8080/file

#上传文件服务域名
domain_file = http://127.0.0.1:8080/file

#上传临时目录(如果没指定路径的话)
eova_upload_temp = /opt/upload/temp
#上传excel临时目录(如果没指定路径的话)
eova_upload_xls = /opt/upload/temp/xls

#编辑器图片
dir_editor = /opt/upload/editor

#7牛服务配置（即使配置，需要配置使用7牛）
qiniu_access_key=gkmv2ZdnnWbmKHTh0HUugMp9M_n4USkqz3_KY2xf
qiniu_secret_key=Ago_-Qfc9ISI5hE04IXeSIG6amOAOkz5zA1nr6ch
qiniu_bucket_name=building-block

#默认生成页面根路径（一旦形成建议不要修改，否则需要把已生成文件copy至新位置）,开发环境可以设置成直接覆盖，生产还是不推荐覆盖
page_default=/pages/
page_default_over=1